# Example environment file for Docker
# Copy this to .env and fill in your values

# Flask secret key - generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your_generated_secret_key_here

# Developer password - backup admin access
DEVELOPER_PASSWORD=your_secure_developer_password_here

# Database encryption key - generate with: python -c "import secrets; print(secrets.token_hex(32))"
# REQUIRED for v1.0.3+ - enables AES-256 SQLCipher database encryption
DB_ENCRYPTION_KEY=your_generated_db_encryption_key_here

# Field encryption key - generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# REQUIRED for v1.0.3+ - enables Fernet field-level encryption for sensitive data
FIELD_ENCRYPTION_KEY=your_generated_field_encryption_key_here

# Flask debug mode - MUST be False in production
FLASK_DEBUG=False

# Optional: Trail Life Connect integration (if using TLC sync)
# TLC_API_KEY=your_tlc_api_key_here

# Optional: Email configuration for password recovery
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=your_email@gmail.com
# MAIL_PASSWORD=your_app_password_here
